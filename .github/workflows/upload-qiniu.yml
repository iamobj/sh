name: upload qiniu

on:
  push:
    paths:
      - '!README.md'

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: 检出代码到本地
        uses: actions/checkout@v2
      
      - name: 测试
        run: |
          echo "${{toJSON(github.event.commits)}}"